                ----todo app----
* data structure
  [
    {title:'todo app',todoItems:[
      { item:item1,isDone:false },
      { item:item2,isDone:false }
      ],isDone:false}
  ]

--done-- * make the home page

--done-- * keep text boxes in home page so user can add their todoItems

--done-- * by submitting it should store in json file.

--done-- * every time it should load from json file and display on that page

--done-- * mark an item done or undone

--done-- * delete one todo item from the file

--done-- * delete one todo list from lists